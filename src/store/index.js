import { createStore } from "vuex";
import openai from "./modules/openai"; // for everything screel

export default createStore({
  state: {},
  getters: {},
  mutations: {},
  actions: {},
  modules: {
    openai,
  },
});
